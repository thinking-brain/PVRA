@*@using DevExpress.Web.Mvc.UI
@Html.DevExpress().DocumentViewer(settings =>{
    // The following settings are necessary for a Report Viewer. 
    settings.Name = "reportViewer1";
    settings.Report = (XtraReport)ViewData["Report"];
    // Callback and export route values specify corresponding controllers and their actions. 
    // These settings are necessary as well. 
    settings.CallbackRouteValues = new { Controller="Reportes", Action="ReportViewerPartial" };
    settings.ExportRouteValues = new { Controller="Reportes", Action="ExportReportViewer" };}).GetHtml()*@

